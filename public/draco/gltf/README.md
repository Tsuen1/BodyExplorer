Draco decoder files copied from `three/examples/jsm/libs/draco/gltf`.

They are required by `DRACOLoader` to load the Draco-compressed Open3DModel GLB files in the browser without relying on an external CDN.
